Программа определяет кратно ли четырем число, введенное пользователем.

#!/bin/bash
echo "r: Введите число"
read num
if (( $num % 4 == 0 ))
then
  echo "Число $num кратно четырем"
else
  echo "Число $num четырем"
fi
